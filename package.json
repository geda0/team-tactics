{
  "name": "ttics",
  "version": "0.0.0",
  "private": true,
  "description": "team-tactics monorepo — tics (protocol) + tdd (pairing) + team-tactics (product)",
  "workspaces": ["packages/*"],
  "scripts": {
    "test": "npm test --workspaces --if-present"
  }
}
